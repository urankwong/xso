name: data
description: 本地存储：drift 数据库、源仓库（纯 Dart）
version: 0.1.0
environment:
  sdk: ^3.5.0
dependencies:
  core:
    path: ../core
  drift: ^2.20.0
dev_dependencies:
  test: ^1.25.0
  drift_dev: ^2.20.0
  build_runner: ^2.4.0
  path: ^1.9.0
